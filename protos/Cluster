cluster found by coust reatil integral from seletct beam this(id)
list desktop from hover coust reatil loop loss beam thi(id)
cluster list integral set upgrade key from select * cat int mett bin
under head set upgarde key cluster found reatil coust select from this(id)
